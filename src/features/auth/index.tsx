import SignInSide from './components/sign-in-side';

// We have just one component in this feature at the moment, but we will add more stay tunned
export default function Auth() {
  return (
    <>
      <SignInSide />
    </>
  );
}
